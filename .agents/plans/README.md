# Plans migrated to Spec Kit

Legacy RouterGo plans were replaced by the canonical Spec Kit-style baseline at:

`specs/001-routergo-platform/`

Agents must use `AGENTS.md`, `.specify/memory/constitution.md`, and that active spec/plan/tasks set. New capabilities receive their own numbered `specs/NNN-*` folder.
